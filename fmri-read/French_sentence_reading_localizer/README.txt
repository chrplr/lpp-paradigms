To run the experiment, type:

    python french_reading_localizer.py 

(you need the python package expyriment.org installed on the computer)

The experimental list (`list1.csv` or `list2.csv` in `lists`) is selected on the bases of the parity of the subject's number.

The duration is 440s (7m20s)

